# 🎮 Epic Free Games Bot

Telegram-бот на базе `aiogram 3.x` для автоматического отслеживания и публикации еженедельных бесплатных игр из Epic Games Store в ваш Telegram-канал.

**Бот:** [@gamefreebie_bot](https://t.me/gamefreebie_bot)  
**Канал:** [@grampsgamer](https://t.me/grampsgamer)

---

## 📌 О проекте

Бот раз в день обращается к публичному API Epic Games Store, находит активные 100% скидки (бесплатные раздачи), формирует красивый объединенный пост с обложками и описанием игр и публикует его в Telegram-канал. 

### Ключевые особенности:
* **Объединение игр (Media Group):** Если в раздаче участвуют несколько игр (обычно две), бот отправляет их **одним сообщением**. Картинки собираются в альбом, а описания и ссылки публикуются как единый структурированный текст под первой картинкой.
* **Защита от дубликатов:** Бот ведет локальную базу отправленных раздач в JSON-файле (`history.json`), чтобы исключить дублирование постов.
* **Автоматическое форматирование:** Описания автоматически обрезаются под лимиты Telegram (1024 символа для подписей к медиафайлам).

---

## ⚙️ Доступные команды бота

Команды работают в личных сообщениях с ботом. Административные команды доступны только пользователям, чьи ID прописаны в параметре `ADMIN_IDS` в файле `.env`.

### Для пользователей:
* `/start` — Запуск бота. Выводит приветственное сообщение, статус пользователя (пользователь/администратор) и показывает личный Telegram ID.

### Для администраторов:
* `/check` — **Безопасное превью.** Запускает немедленный поиск раздач в Epic Games API и присылает готовый объединенный пост (или альбом) **напрямую в ЛС администратору** (в канал ничего не публикуется). Позволяет оценить внешний вид поста (картинки, верстку, ссылки).
* `/check channel` — **Принудительная публикация.** Запускает поиск раздач и принудительно публикует их в Telegram-канал в виде альбома (игнорируя историю ранее опубликованных игр в `history.json`).
* `/help` — Выводит справку по административным командам.

---

## 🚀 Быстрый старт и запуск

### 1. Установка зависимостей
Установите библиотеки из файла `requirements.txt`:
```bash
py -m pip install -r requirements.txt
```

### 2. Настройка окружения
Создайте или отредактируйте файл `.env` в корне проекта:
```env
# Токен бота от @BotFather
BOT_TOKEN=ваш_токен_бота

# ID или юзернейм канала для публикаций
CHANNEL_ID=@grampsgamer

# Список ID администраторов бота через запятую
ADMIN_IDS=1205125640,987654321

# Время ежедневной автоматической проверки раздач (в формате HH:MM)
CHECK_TIME=19:00
```
> ⚠️ **Важно:** Бот считывает настройки из `.env` только в момент своего запуска. Если вы изменили `.env`, бота нужно обязательно **перезапустить** (остановить процесс через `Ctrl + C` в терминале и запустить заново).

### 3. Запуск бота в VS Code
1. Откройте файл `src/main.py`.
2. Нажмите на иконку **зеленого треугольника** (Run Python File) в правом верхнем углу VS Code.
3. Бот начнет работу. В терминале отобразятся логи запуска.